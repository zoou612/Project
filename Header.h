#include <stdio.h>
/*

void main()
{
	// 사분면
	// 제 1 사분면 : (+x, +y)
	// 제 2 사분면 : (-x, +y)
	// 제 3 사분면 : (-x, -y)
	// 제 4 사분면 : (+x, -y)
	
	// Y 절편 : (x[0], y)
	// Y 절편 : (x, y[0])
	// 원점 : (x[0], y[0])

	int x = -2;
	int y = 10;

	if (x > 0 && y > 0)
	{
	   printf("제 1 사분면");
	}
	els if (x < 0 && y > 0)
	{
	   printf("제 2 사분면");
	}
	else if (x < 0 && y < 0)
	{
	   printf("제 3 사분면");
	}
	else if (x > 0 && y <0)
	{
	   printf("제 4 분면");
	}
	else if (x == 0 && y != 0)
	{
	   Printf("Y 절편");
	}
	else if (x != 0 && y == 0)
	{
	   printf("X 절편");
	}
	else
	{
	   printf("원점");
	}

	// 1 사분면

	// 논리 연산자
	// 1 = 참
	// 0 = 거짓
	// && (AND) : 두개의 조건 모두 참이라면 1을 반환합니다.
	// || (OR) : 두 개의 조건에 하나라도 참이면 조건을 실행합니다.

	int x = 10;
	int y = 20;

	if (x == 10 && y == 20)
	{
		printf("논리 연산 AND가 참입니다.")
	if (5 < 10)
	}
	    printf(if문 안에는 조건문입니다.\n");

	in (x == 10 || y ==25)
	{
	*/
	

